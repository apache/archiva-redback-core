language: java
jdk:
  - openjdk8
  - oraclejdk8

script: "mvn clean install"

cache:
    directories:
    - $HOME/.m2
branches:
    except:
        - gh-pages
notifications:
    email:
        - olamy@apache.org
